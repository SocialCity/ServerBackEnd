run from this folder, copy the whole thing onto disk. If not, just make sure the resources folder and the jars are in the same folder for running. server.jar can just be called and run. updater.jar needs a string argument passed to it. That arguement must be the name of the most recent collection of tweets. That collection must be stored within the tweetInfo database, seen in the dump.
